<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alin.supercross">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.CallActivity" android:process=":kkkkk"></activity>
        <activity android:name=".test.ThreeActivity" android:process=":kkkkk"></activity>
        <!--<service android:name=".core.CrossService" />-->
        <service android:name="com.alin.cross.core.CrossService"></service>
        <service android:name="com.alin.cross.core.CrossService$CrossService0"></service>
        <service android:name="com.alin.cross.core.CrossService$CrossService1"></service>

    </application>

</manifest>